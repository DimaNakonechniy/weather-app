import { createTheme } from "@mui/material";

import { MuiButton, MuiPaper } from "./components";

const theme = createTheme({
  components: {
    MuiButton,
    MuiPaper,
  },
});

export default theme;
